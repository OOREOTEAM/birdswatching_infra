node_name = "consul-server"
server = true
bootstrap_expect = 1

ui_config {
  enabled = true
}

data_dir = "consul/data"

bind_addr = "{{ ansible_default_ipv4.address }}"

addresses {
  http = "0.0.0.0"
}

ports {
  dns = 8600
}

acl {
  enabled = false
}

