[Unit]
Description=Consul server
Requires=network-online.target
After=network-online.target

[Service]
User=consul
Group=consul
ExecStart=/usr/local/bin/consul agent -server -config-file=/etc/consul.d/consul.hcl
Restart=on-failure

[Install]
WantedBy=multi-user.target